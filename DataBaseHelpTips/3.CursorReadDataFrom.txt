1. Как будем двигаться по данным

if(cursor.moveToFirst()){
//... первая запись набора
};
if(cursor.moveToLast()){
//... последняя запись набора
};
if(cursor.moveToPrevious){
//... предыдущая запись набора
};
if(cursor.moveToNext){
//... следующая запись набора
};

2. Как будем читать данные из курсоров
Для чтения текущей записи из курсора
используются методы get*();
//--> возможно нужно будет применять метод parseInt()
getString(); //Возвращает значение столбцов в формате String
getInt(); //Возвращает значение столбца в формте int
_____________________________
Cursor cursor = db.query("DRINK", new String[] {"NAME", "DESCRIPTION", "IMAGE_RESOURCE_ID"}, "_id = ?", new String[] {Integer.toString(1)}, null, null, null);
String name = cursor.getString(0);
int imageResource = cursor.getInt(2);

3. Закрываем курсор и б.д.
close(); //закрываемся
cursor.close();
db.close();